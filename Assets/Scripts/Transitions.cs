using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Transitions : MonoBehaviour
{/*
    public Text mensajeText;

    private void Start()
    {
        StartCoroutine(PrimeraTransicion());
        Enemigo.EnemigoDerrotado += VerificarTransicionSegunda;
    }

    IEnumerator PrimeraTransicion()
    {
        yield return new WaitForSeconds(32f);
        MostrarMensaje("Ya terminaste tu entrenamiento. ¡Puedes enfrentar a los enemigos!");
    }

    void VerificarTransicionSegunda()
    {
        if (Enemigo.EnemigosDerrotados >= 5)
        {
            Enemigo.EnemigoDerrotado -= VerificarTransicionSegunda;
            StartCoroutine(SegundaTransicion());
        }
    }

    IEnumerator SegundaTransicion()
    {
        yield return null;
        MostrarMensaje("Oh no, apareció otro enemigo más fuerte. Debes matarlo.");
    }

    void MostrarMensaje(string mensaje)
    {
        mensajeText.text = mensaje;

        // Aquí también podrías implementar animaciones, efectos de sonido, etc.

        // Después de mostrar el mensaje, puedes continuar con la lógica del juego.
    }
}

    */
}

